# Cookie session data (contains login credentials)
.cookies/

# Debug screenshots
*.png

# Node modules
node_modules/

# Local configuration cache (contains server paths, never commit)
.local-config.json
